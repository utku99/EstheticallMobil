import { createSlice } from "@reduxjs/toolkit";

const comonSlice = createSlice({
    name: "common",
    initialState: {
    },
    reducers: {

    }
})

export default comonSlice.reducer
export const { } = comonSlice.actions 